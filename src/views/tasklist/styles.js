import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container:{
        flex:1
    },
    title:{
        alignSelf: 'center',
        fontSize: 34,
        marginTop: 12,
        fontWeight: 'bold',
    }
})

export default styles